## This is a practice engine.
- You have to pull this repo's newest version: git pull
- You have to install the requirements given in requirements.txt using command: pip install -r requirements.txt
- You have to run the app using command: streamlit run .\engine\app.py
- And then send the screenshot of the dashboard in whatsapp.